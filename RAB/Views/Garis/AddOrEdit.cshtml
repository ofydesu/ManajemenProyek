@model RAB.Models.Utama.Garis

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>Garis</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="GarisId" />
            <div class="form-group">
                <label asp-for="AwalId" class="control-label"></label>
                <select asp-for="AwalId" class="form-control" asp-items="ViewBag.AwalId"></select>
                <span asp-validation-for="AwalId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="AkhirId" class="control-label"></label>
                <select asp-for="AkhirId" class="form-control" asp-items="ViewBag.AkhirId"></select>
                <span asp-validation-for="AkhirId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
